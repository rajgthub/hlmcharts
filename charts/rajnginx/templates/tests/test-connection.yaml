apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rajnginx.fullname" . }}-test-connection"
  labels:
{{ include "rajnginx.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rajnginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
